# Problem A: Sum of Squares Inversion

Find integer n such that:
n * (n + 1) * (2 * n + 1) / 6 = k

Input:
T queries, each with a long integer k.

Output:
For each query, print n, or -1 if no such integer n exists.